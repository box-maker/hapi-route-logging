{
  "restartable": "rs",
  "verbose": false,
  "watch": [
    "src/"
  ],
  "env": {
    "NODE_ENV": "development"
  },
  "exec": "yarn run lint && yarn run transpile && yarn run translations && node",
  "ext": "js json"
}